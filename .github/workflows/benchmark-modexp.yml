name: EIP-8200 MODEXP Benchmark

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: eip8200-modexp-benchmark-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    uses: ./.github/workflows/_benchmark-track.yml
    with:
      track: modexp
